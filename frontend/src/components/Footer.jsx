const Footer = () => (
  <footer className="container" style={{ textAlign: "center", marginTop: "40px", color: "#64748b" }}>
    <p>Todo App built with React, Express, and MongoDB.</p>
  </footer>
);

export default Footer;
